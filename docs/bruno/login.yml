info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{ORIGIN}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "example1@gmail.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
