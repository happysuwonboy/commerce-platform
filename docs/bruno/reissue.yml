info:
  name: reissue
  type: http
  seq: 3

http:
  method: POST
  url: "{{ORIGIN}}/api/auth/reissue"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
