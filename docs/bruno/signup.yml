info:
  name: signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{ORIGIN}}/api/auth/signup"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "exp1@example.com",
        "password": "12378",
        "name": "exp1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
